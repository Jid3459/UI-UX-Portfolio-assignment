Place your resume here as `resume.pdf`.

Any file inside this `public/` folder is served at the site root at build time.
So `public/resume.pdf` is reachable at https://yourdomain.com/resume.pdf.

The Hero section's "Download Resume" and "View Resume" buttons and the
resume icon in the top-right header all link to /resume.pdf.

Suggested filename in the file system: resume.pdf
Suggested download filename (what the user sees after clicking Download):
Jidneya-Kadam-Resume.pdf  (configured via the `download` attribute on the link)

That's it — drop the PDF here, commit, and push. Vercel will redeploy.
